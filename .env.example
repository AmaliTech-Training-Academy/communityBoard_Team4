# Docker Compose Environment Variables
# Copy this file to .env and update with your values

# Database Configuration
POSTGRES_DB=communityboard
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# Backend Configuration
BACKEND_PORT=8080
JWT_SECRET=communityboard-secret-key-amalitech-2024-change-in-production
SPRING_PROFILES_ACTIVE=docker
SPRING_MAIL_HOST=mailpit
SPRING_MAIL_PORT=1025
SPRING_MAIL_USERNAME=
SPRING_MAIL_PASSWORD=
EMAIL_VERIFICATION_ENABLED=true
EMAIL_VERIFICATION_FROM=no-reply@communityboard.local
EMAIL_VERIFICATION_URL=http://localhost:3000/verify-email
EMAIL_NOTIFICATIONS_ENABLED=true
EMAIL_NOTIFICATIONS_FROM=no-reply@communityboard.local
FRONTEND_BASE_URL=http://localhost:3000

# Frontend Configuration
FRONTEND_PORT=3000
REACT_APP_API_BASE_URL=http://localhost:8080/api

# Development Configuration
COMPOSE_PROJECT_NAME=communityboard
COMPOSE_FILE=docker-compose.yml

# Optional Services
REDIS_PORT=6379
REDIS_PASSWORD=

# Monitoring (if enabled)
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
GRAFANA_ADMIN_PASSWORD=admin

# Logging
LOG_LEVEL=INFO
LOG_FILE_PATH=./logs

# Security
ENABLE_HTTPS=false
SSL_CERT_PATH=
SSL_KEY_PATH=

# Backup Configuration
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=7
BACKUP_S3_BUCKET=

# Performance
JAVA_OPTS=-Xmx512m -Xms256m
NODE_OPTIONS=--max-old-space-size=2048

# Development Tools
ENABLE_DEBUG=false
ENABLE_HOT_RELOAD=true
ENABLE_MOCK_DATA=false
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025
