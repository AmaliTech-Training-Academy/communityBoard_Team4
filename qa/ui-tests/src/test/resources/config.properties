# ---------------------------------------------------------------------------
# CommunityBoard UI Test Configuration
# ---------------------------------------------------------------------------
# Replace email/password with valid test account credentials before running.
# ---------------------------------------------------------------------------

base.url=http://community-board-alb-1961944079.eu-west-1.elb.amazonaws.com

# Test account – must already exist in the application
valid.email=nana.addo@test.com
valid.password=password123 #pragma: allowlist secret

# Browser settings
browser=chrome
# Set to false to run with a visible browser window (useful during development)
headless=false

# Timeouts (seconds)
implicit.wait=10
explicit.wait=15
